#include<stdio.h>
#include<stdlib.h>
using namespace std;


/*
    复习：欧几里得算法就是 gcd(a,b)转化为求解gcd(b,a%b)
    b==0时返回a 此时a就是gcd

    扩展欧几里得算法：ax+by=gcd(a,b)
    方程ax+by=c求解
    ax=c(mod m)求解
    逆元的求解
    (b/a)%m的计算
*/

int main()
{

    

    
    return 0;
}
